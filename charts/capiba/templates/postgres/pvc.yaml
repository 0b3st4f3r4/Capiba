{{- if and .Values.postgresql.enabled .Values.postgresql.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "capiba.postgresql.fullname" . }}-data
  annotations:
    helm.sh/resource-policy: keep
  labels:
    {{- include "capiba.labels" . | nindent 4 }}
    {{- include "capiba.componentLabels" (dict "component" "postgresql" "context" .) | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.global.servicesDataPath }}
  storageClassName: ""
  volumeName: {{ include "capiba.postgresql.fullname" . }}-data-pv
  {{- else if or .Values.global.storageClass .Values.postgresql.persistence.storageClass }}
  storageClassName: {{ include "capiba.storageClass" (dict "global" .Values.global "persistence" .Values.postgresql.persistence) }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.postgresql.persistence.size }}
{{- end }}
